# WebSSH Terminal Environment Configuration

# Encryption key for credential security (32-character hex string)
# If not provided, a random key will be generated (not recommended for production)
ENCRYPTION_KEY=your-32-character-hex-encryption-key-here

# Logging level (ERROR, WARN, INFO, DEBUG)
LOG_LEVEL=INFO

# Server port (default: 3000)
PORT=3000

# Node environment
NODE_ENV=development

# Maximum number of concurrent SSH sessions per IP (default: 10)
MAX_SESSIONS_PER_IP=10

# Session timeout in minutes (default: 60)
SESSION_TIMEOUT_MINUTES=60

# Rate limiting: maximum connection attempts per IP per window
RATE_LIMIT_MAX_ATTEMPTS=5

# Rate limiting: time window in minutes
RATE_LIMIT_WINDOW_MINUTES=5
